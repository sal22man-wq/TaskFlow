# قاعدة البيانات
DATABASE_URL="postgresql://username:password@localhost:5432/taskflow"
PGUSER=username
PGPASSWORD=password
PGDATABASE=taskflow
PGHOST=localhost
PGPORT=5432

# إعدادات الجلسة
SESSION_SECRET="your-super-secret-session-key-here"

# إعدادات الخادم
PORT=5000
NODE_ENV=development

# ملاحظات للتشغيل خارج Replit:
# 1. أنشئ قاعدة بيانات PostgreSQL
# 2. حدث DATABASE_URL بمعلومات قاعدة البيانات الصحيحة
# 3. غير SESSION_SECRET لقيمة عشوائية آمنة
# 4. شغل: npm install && npm run db:push && npm run dev